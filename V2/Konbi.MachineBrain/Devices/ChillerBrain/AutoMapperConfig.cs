using System;

namespace ChillerBrain
{
    public class AutoMapperConfig
    {
        public static void Initialize()
        {
            //Mapper.Initialize(cfg =>
            //{
                
            //    cfg.CreateMap<ErrorLog, KonbiBrain.DocumentDb.Entities.ErrorLog>()
            //     .ForMember(x => x.Id, o => o.MapFrom(d => d.MachineId))
            //     .ForMember(x => x.Date, o => o.UseValue(DateTime.Now));

            //});
        }
    }
  
}
